spring:
  datasource:
    url: jdbc:postgresql://localhost:49155/postgres
    username: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: true

logging:
  level:
    org:
      springframework:
        security: DEBUG